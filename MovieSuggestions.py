# Find the movies that have higher ratings than the movie selected. You are given a movie(node) and a number of movies
# to find in relation to that movie. If the number of movies is more than the amount of movies stored, return a
# list of all the movies.
#
# Use a graph to figure out how to traverse and find a list of movie suggestions.

# test case:
# (1, 1.6), 2
# (1, 6.4), 10



class Movie(object):
  self.info = None
  self.rating = None

  def getID(self):


  def getRating(self):


  def getMovieSuggestion(self):
